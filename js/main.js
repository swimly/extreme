;(function(){
  var mySwiper = new Swiper ('.banner', {
    direction: 'horizontal',
    loop: true,
    autoplay: 3000,
    speed: 500,
    // 如果需要分页器
    pagination: '.swiper-pagination'
  }) 
  var mySwiper = new Swiper ('.advert', {
    direction: 'horizontal',
    loop: true,
    autoplay: 4000,
    speed: 300,
    // 如果需要分页器
    pagination: '.swiper-pagination'
  })
})()